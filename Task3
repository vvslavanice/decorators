'''
task 3 я пока не понимаю как делать, а брать чужие идеи не хочу, но я оставлю небольшие пометки, чтобы вы понимали, что я + - суть понял

''' 


'''
Это я буду использовать для понимания типа
'''
def arg_rules(type_: type, max_length: int, contains: list):
    def type_of_message(func):
        def wrapper(type_):
            newlistik = func(type_)
            for words in type_:
                if words != str:
                    newlistik == str(newlistik)
            return newlistik
        return wrapper
    return type_of_message

'''
Создам отдельный список стоп символов которые не будут попадать в конечное слово
'''

    def variable(func):
        def wrapper(list):
            new_list = func(list)
            for word in words:
                if word in new_list:
                    new_list = new_list.replace(word, ' ')
            return new_list

        return wrapper

    return variable
